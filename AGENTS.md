## Agent skills

### Issue tracker

Issues live as GitHub Issues in `jny986/homelab`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
